/**
 * Shared TypeScript types + Zod schemas used by multiple packages.
 *
 * Rule of thumb: if two packages need the same type, it goes here.
 * If only one package needs it, co-locate it there instead.
 */

export * from './result';

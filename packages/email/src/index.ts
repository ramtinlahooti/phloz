// Scaffold: real content lands in Step 5 (see docs/NEXT-STEPS.md).
// Public API surface will include sendInvitation, sendPortalMagicLink,
// sendPasswordReset, and the inbound webhook parser.
export const PACKAGE_NAME = '@phloz/email';
